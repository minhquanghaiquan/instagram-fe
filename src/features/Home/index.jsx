import React from 'react';
import PropTypes from 'prop-types';
import LoginForm from 'features/Authentication/components/LoginForm';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div>WELLCOME TO INSTAGRAM</div>
    );
}

export default Home;